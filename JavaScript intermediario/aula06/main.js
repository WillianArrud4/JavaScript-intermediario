
 let quadrado = document.querySelector('.quadrado')

 function minhaFuncao(evento) {
    console.log(evente.key)
 }

 // quadrado.onclick = minhaFuncao

 // quandrado.onmousemove = minhaFuncao = aplica um click do mause a cada movimento dentro do objeto

 // quadrado.onmouseenter = minhaFuncao = aplica un click do mausa a cada vez que o mause pasar por cima do objeto

 // quadrado.onmouseout = minhaFuncao = aplica um click que quando o mause sair de cima do objeto ele conta um click

 // window.addEventListener('resize', minhaFuncao) 

 window.addEventListener('keypress', minhaFuncao)